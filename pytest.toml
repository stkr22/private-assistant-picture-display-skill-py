[pytest]
minversion = "9.0"
addopts = ["-ra", "-q"]
testpaths = [
    "tests",
    "integration",
]
markers = [
    "integration: marks tests as integration tests requiring external services (PostgreSQL, MQTT)",
]
asyncio_mode = "auto"
